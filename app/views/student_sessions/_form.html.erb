<%= form_for(@student_session) do |f| %>
  <% if @student_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_session.errors.count, "error") %> prohibited this student_session from being saved:</h2>

      <ul>
      <% @student_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% if current_student %>
        
  <%= link_to'profile',:profile %>

  <% else %>
  
  <div class="field" style="color:white"> 
     <%= f.label :username %><br />
    <center><%= f.text_field :username, :class =>"tb7" %></center>
  </div>
  
  <div class="field" style="color:white">
     <%= f.label :password,:for=>"password" %><br />
    <center><%= f.password_field :password, :class=>"tb7" %></center>
  </div>
  <div class="actions">
    <%= f.submit "LOGIN", :class=>"button", :style=>"float:right;" %>
  </div>
  <% end %>
<% end %>
