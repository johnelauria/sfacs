<h1>Listing cashier_sessions</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cashier_sessions.each do |cashier_session| %>
  <tr>
    <td><%= cashier_session.username %></td>
    <td><%= cashier_session.password %></td>
    <td><%= link_to 'Show', cashier_session %></td>
    <td><%= link_to 'Edit', edit_cashier_session_path(cashier_session) %></td>
    <td><%= link_to 'Destroy', cashier_session, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cashier session', new_cashier_session_path %>
