<%= form_for(@cashier_session) do |f| %>
  <% if @cashier_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cashier_session.errors.count, "error") %> prohibited this cashier_session from being saved:</h2>

      <ul>
      <% @cashier_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
