<h1>Listing teacher_sessions</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @teacher_sessions.each do |teacher_session| %>
  <tr>
    <td><%= teacher_session.username %></td>
    <td><%= teacher_session.password %></td>
    <td><%= link_to 'Show', teacher_session %></td>
    <td><%= link_to 'Edit', edit_teacher_session_path(teacher_session) %></td>
    <td><%= link_to 'Destroy', teacher_session, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Teacher session', new_teacher_session_path %>
